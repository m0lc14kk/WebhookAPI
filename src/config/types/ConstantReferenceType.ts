type ConstantReferenceType = {
    readonly description: string,
    readonly typeFlags: ({
        flagName: string,
        flagDescription: string
    })[]
};

export type { ConstantReferenceType };